# Cutout eval (illo v0.24.0)

Six-case cutout benchmark (Codex + OpenRouter models/methods). Live gallery:

**https://d2a97653.ht-ml.app/**

Artifacts: `index.html`, `results.json`, prompts, and render outputs from the v0.24.0 eval run.

Regenerate:

```bash
python3 .github/scripts/cutout_eval.py
```
